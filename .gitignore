# straight.el package manager (rebuilt automatically, don't track)
straight/

# Native compilation cache
eln-cache/

# Auto-save list (buffer recovery info, not config)
auto-save-list/

# Minibuffer history (savehist)
history

# Compiled bytecode, in case any sneak in
*.elc
*.eln

# Backup/auto-save files Emacs creates next to edited files
*~
\#*\#
.\#*

# OS cruft
.DS_Store